services:
  - type: web
    name: quantum-routes-api
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: IBM_QUANTUM_TOKEN
        value: CTHO-3HwGTb6NhNrD_CM1qtHFscizWIGBuIT3boGK_s0
      - key: PYTHON_VERSION
        value: 3.10.0

# Made with Bob
